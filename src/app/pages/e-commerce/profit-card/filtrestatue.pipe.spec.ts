import { FiltrestatuePipe } from './filtrestatue.pipe';

describe('FiltrestatuePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrestatuePipe();
    expect(pipe).toBeTruthy();
  });
});
