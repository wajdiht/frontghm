import { DateFiltrePipe } from './date-filtre.pipe';

describe('DateFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
