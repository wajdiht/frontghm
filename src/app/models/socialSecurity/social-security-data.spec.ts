import { SocialSecurityData } from './social-security-data';

describe('SocialSecurityData', () => {
  it('should create an instance', () => {
    expect(new SocialSecurityData()).toBeTruthy();
  });
});
